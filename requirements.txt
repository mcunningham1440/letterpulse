Django>=5.0
pandas>=2.0
openai>=1.0
anthropic>=0.103.0     # Anthropic Claude API client (second LLM provider, fallback target)
beautifulsoup4>=4.12
pydantic>=2.0
asgiref>=3.7
psycopg2-binary>=2.9  # PostgreSQL driver
python-dotenv>=1.0     # For .env file support
django-allauth>=0.61    # Authentication with email verification
aiohttp>=3.9           # Async HTTP client
Levenshtein>=0.25      # String matching
gunicorn>=21.0         # WSGI HTTP server for production
whitenoise>=6.7        # Static file serving for production
cryptography>=42.0     # For encrypting sensitive data at rest
pytz>=2024.1          # Timezone handling for migrations
perplexityai>=0.20.0   # Perplexity search API client

# Test-only dependencies
pytest>=8.0
pytest-django>=4.7
pytest-asyncio>=0.23
aioresponses>=0.7
coverage>=7.4